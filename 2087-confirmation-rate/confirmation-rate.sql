# Write your MySQL query statement below
select u.user_id , coalesce(round((sum(case when c.action = 'confirmed' then 1 end) / count(action)),2),0) as confirmation_rate from signups as u left join confirmations as c on u.user_id = c.user_id group by u.user_id;